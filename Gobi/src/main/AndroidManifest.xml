<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.teammark.parkingmanagement">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-sdk tools:overrideLibrary="com.google.zxing.client.android" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.TeamMark">

        <activity android:name=".CalculateParkingPaymentActivity"/>
        <activity android:name=".CreateParkingAreaActivity"/>
        <activity android:name=".CreateParkingReservationActivity"/>
        <activity android:name=".DetailsParkingAreaActivity"/>
        <activity android:name=".UpdateParkingReservationActivity"/>
        <activity android:name=".ViewParkingAreaActivity"/>
        <activity android:name=".ViewQRCodeActivity"/>
        <activity android:name=".ViewReservationActivity"/>

        <activity android:name=".FindParkingSlotActivity">
            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>

        <activity android:name=".MainActivity">
            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>

    </application>

</manifest>